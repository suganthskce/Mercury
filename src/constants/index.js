/**
 * Constants used across application.
 */

const konstants = (() => {
    return {
        DATE_FORMAT: {
            'DD/MM/YYYY': 'DD/MM/YYYY',
            'DD-MM-YYYY': 'DD-MM-YYYY'
        }
    };
})();

export default konstants;
